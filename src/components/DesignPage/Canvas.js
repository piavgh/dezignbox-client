import React from 'react';
import PropTypes from 'prop-types';
import {
    Row,
    Col
} from 'reactstrap';

const Canvas = props => {
    return <Row>
        <Col xs={12}>
            Canvas
        </Col>
    </Row>;
};

Canvas.propTypes = {};

export default Canvas;
